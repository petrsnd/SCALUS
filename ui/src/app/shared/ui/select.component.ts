import { Component, EventEmitter, Input, Output } from '@angular/core';
import { CommonModule } from '@angular/common';
@Component({ selector: 'ui-select', standalone: true, imports: [CommonModule], template: `<select class="select" [value]="value ?? ''" [disabled]="disabled" (change)="valueChange.emit($any($event.target).value || null)"><option *ngIf="placeholder" value="">{{ placeholder }}</option><option *ngFor="let option of options" [value]="option.value">{{ option.label }}</option></select>`, styles: [`.select{width:100%;height:36px;padding:0 var(--oi-space-m);border:1px solid var(--oi-border);border-radius:var(--oi-radius);background:var(--oi-bg-primary);color:var(--oi-content-primary);font-size:13px;font-weight:500}.select:hover:not(:disabled){border-color:var(--oi-border-strong)}.select:disabled{opacity:.6;border-style:dashed}`] })
export class UiSelectComponent { @Input() options: { label: string; value: string }[] = []; @Input() value: string | null | undefined = ''; @Input() placeholder = ''; @Input() disabled = false; @Output() valueChange = new EventEmitter<string | null>(); }
