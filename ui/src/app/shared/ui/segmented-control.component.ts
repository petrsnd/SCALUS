import { Component, EventEmitter, Input, Output } from '@angular/core';
import { CommonModule } from '@angular/common';
@Component({ selector: 'ui-segmented-control', standalone: true, imports: [CommonModule], template: `<div class="seg" role="group"><button type="button" *ngFor="let option of options" [class.active]="option.value === value" (click)="valueChange.emit(option.value)">{{ option.label }}</button></div>`, styles: [`.seg{display:inline-flex;background:var(--oi-bg-secondary);border:1px solid var(--oi-border-muted);border-radius:var(--oi-radius);padding:2px;flex:none}.seg button{border:none;background:none;color:var(--oi-content-secondary);padding:6px 14px;border-radius:calc(var(--oi-radius) - 2px);font-size:13px;font-weight:600}.seg button.active{background:var(--oi-bg-primary);color:var(--oi-content-primary);box-shadow:var(--oi-shadow-low)}`] })
export class UiSegmentedControlComponent { @Input() options: { label: string; value: string }[] = []; @Input() value = ''; @Output() valueChange = new EventEmitter<string>(); }
